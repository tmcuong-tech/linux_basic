cài đặt R và R Studio

cài đặt R bằng terminal
	sudo apt install r-base-core

cài đặt ứng dụng trên web
	cài đặt R studio trực tiếp trang web posit
	chọn phiên bản R studio desktop
	chọn file.deb tương ứng với hệ điều hành -> download
	mở terminal
	truy cập vào thư mục chứa file đã tải về
	cài đặt ứng dụng bằng lệnh
		sudo dpkg -i tên_file_ứng_dụng.deb
	
